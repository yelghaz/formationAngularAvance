import { TootipDirective } from './tootip.directive';

describe('TootipDirective', () => {
  it('should create an instance', () => {
    const directive = new TootipDirective();
    expect(directive).toBeTruthy();
  });
});
